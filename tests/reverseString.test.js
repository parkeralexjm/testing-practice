// Take a string and reverse it

const reverseString = require('../codes/reverseString')

test('Take the string and reverse it', () => {
  expect(reverseString('reverse this string')).toBe('gnirts siht esrever');
});